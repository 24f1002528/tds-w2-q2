name: Hello World Action  # Name shown in GitHub UI

on: [push]  # Triggers on any push to the repo

jobs:
  test:
    runs-on: ubuntu-latest  # Free virtual machine (runner) to execute on
    steps:
      - name: Checkout code  # Step 1: Grabs your repo files
        uses: actions/checkout@v4  # Reusable GitHub Action

      - name: 24f1002528@ds.study.iitm.ac.in  # Your required step (change nothing here)
        run: echo "Hello from my GitHub Action! This ran on $(date)."  # Simple bash command
